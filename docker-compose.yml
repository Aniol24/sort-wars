version: '3.8'

services:
  sort-wars:
    build: .
    platform: linux/arm64
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      - ./logs:/app/logs
    restart: unless-stopped
